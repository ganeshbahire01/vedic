import ContactUs from "@/components/ContactUs";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ContactUs />
    </div>
  );
};

export default page;

import AboutUs from "@/components/AboutUs";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AboutUs />
    </div>
  );
};

export default page;

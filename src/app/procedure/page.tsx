import Treatments from "@/components/treatments";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Treatments />
    </div>
  );
};

export default page;
